# Core app
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
jinja2>=3.0
# Pipeline / dataset
PyYAML>=6.0
openpyxl>=3.1
# Optional: Postgres in production (uncomment)
# psycopg2-binary>=2.9
# Network sources use the standard library (urllib) — no extra deps.
